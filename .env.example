# Copy this file to .env and adjust as needed.
# .env itself is gitignored so machine-specific / secret values
# never get committed.

# Base URL of the Spring Boot backend
VITE_API_BASE_URL=http://localhost:8080/api
